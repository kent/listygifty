# Footer Built to Crafted Plan

## Current State

`apps/web/src/components/layout/Footer.tsx` currently renders a footer containing the `ewakened.com` anchor. In this checkout, the footer text shown in the ticket (`© 2026 · Built by Kent Fenwick · ewakened.com`) is not present in the file; the only JSX text content is the anchor label `ewakened.com`.

## Required Change

When applying the UI change, update only the literal JSX text word `Built` to `Crafted` so the footer reads:

```tsx
© 2026 · Crafted by Kent Fenwick · ewakened.com
```

Do not modify any anchor tags, `href` values, or `className` values.

## Verification

After deploy, verify the production page contains the updated copy:

```bash
curl -fsSL https://listygifty.com | grep -F "Crafted by"
```
