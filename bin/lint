#!/usr/bin/env bash

set -euo pipefail

ROOT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")/.." && pwd)"

cd "${ROOT_DIR}/apps/api"
bin/rubocop

cd "${ROOT_DIR}"
# Turbo builds the shared packages before linting the web app.
npm run lint
npm --prefix apps/web run typecheck
